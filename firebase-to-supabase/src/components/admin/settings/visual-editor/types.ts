import { LABEL_PLACEHOLDERS, type PlaceholderDefinition, type PlaceholderKey, type PlaceholderKind } from '@/lib/printing/labelPlaceholders';
import type { VisualElement, VisualEditorState, VisualElementType, VisualEditorData } from '@/lib/printing/visualLayoutTypes';

export type { PlaceholderDefinition, PlaceholderKey, PlaceholderKind, VisualElement, VisualEditorState, VisualElementType, VisualEditorData };
export { LABEL_PLACEHOLDERS };
