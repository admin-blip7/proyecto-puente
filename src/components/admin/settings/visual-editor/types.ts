import { LABEL_PLACEHOLDERS, type PlaceholderDefinition, type PlaceholderKey, type PlaceholderKind, type PlaceholderScope } from '@/lib/printing/labelPlaceholders';
import type { VisualElement, VisualEditorState, VisualElementType, VisualEditorData } from '@/lib/printing/visualLayoutTypes';

export type { PlaceholderDefinition, PlaceholderKey, PlaceholderKind, PlaceholderScope, VisualElement, VisualEditorState, VisualElementType, VisualEditorData };
export { LABEL_PLACEHOLDERS };
